# something to do

Just let player play randomly, with its In, Middles and Out layers.

For each generation, you must save player's weights.

Save it all in a CSV file.

## 

Get data from game (sensors). It is the IN.

MIDDLEWARES are neurons weights, for each layer.

OUT is the last MIDDLEWARE layer's result. 

Save MIDDLEWARE weights, for each generation.
